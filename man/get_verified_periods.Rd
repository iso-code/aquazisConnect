% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{get_verified_periods}
\alias{get_verified_periods}
\title{Retrieve verified periods for multiple AQUAZIS time series}
\usage{
get_verified_periods(
  hub,
  zrids,
  begin,
  end,
  intervall = "l",
  stepsize = 30,
  max_retries = 5
)
}
\arguments{
\item{hub}{Character string. Base URL of the AQUAZIS hub.}

\item{zrids}{Character or numeric vector. One or more ZRID identifiers of time series.}

\item{begin}{POSIXct or character. Start date/time of the query period.}

\item{end}{POSIXct or character. End date/time of the query period.}

\item{intervall}{Character string. Interval type:
\code{"l"} for left time intervall of measurement or \code{"r"} for right intervall of measurement.}

\item{stepsize}{Integer. Step size in days used for retrying data retrieval
if insufficient values are returned. Default is 30.}

\item{max_retries}{Integer. Maximum number of retry attempts per ZRID. Default is 5.}
}
\value{
A tibble containing the verified periods for all requested ZRIDs.
Each row corresponds to one ZRID with its associated metadata and verified date range.
}
\description{
This function iterates over a list of ZRIDs and determines the latest verified
time for each series by calling \code{\link[=get_az_valid_to]{get_az_valid_to()}}. The results are combined
into a single tibble.
}
\details{
The function calls \code{\link[=get_az_valid_to]{get_az_valid_to()}} for each element in \code{zrids}. If the data
cannot be retrieved immediately, retries are performed with increasing back-off
using \code{stepsize} until either valid data is found or the maximum number of retries
(\code{max_retries}) is reached.
}
\examples{
\dontrun{
hub_url <- "http://example-hub"
zrids <- c("12345", "67890")
begin <- Sys.time() - 60*60*24*30  # 30 days ago
end <- Sys.time()

verified <- get_verified_periods(
  hub = hub_url,
  zrids = zrids,
  begin = begin,
  end = end
)
print(verified)
}

}
