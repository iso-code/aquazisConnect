% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{get_verified_periods_parallel}
\alias{get_verified_periods_parallel}
\title{Retrieve verified periods in parallel}
\usage{
get_verified_periods_parallel(
  hub,
  zrids,
  begin,
  end,
  intervall = "l",
  stepsize = 30,
  max_retries = 5,
  workers = 4
)
}
\arguments{
\item{hub}{A connection or identifier for the data hub used by \code{get_az_valid_to()}.}

\item{zrids}{A vector of ZRID values for which to retrieve data.}

\item{begin}{Start date of the query period (e.g. \code{"2024-01-01"}).}

\item{end}{End date of the query period (e.g. \code{"2024-12-31"}).}

\item{intervall}{Character string specifying the query interval
(default: \code{"l"}; depends on the definition in \code{get_az_valid_to()}).}

\item{stepsize}{Step size for the query (default: \code{30}).}

\item{max_retries}{Maximum number of retry attempts in case of errors
(default: \code{5}).}

\item{workers}{Number of parallel worker processes (default: \code{4}).}
}
\value{
A \pkg{tibble} containing the combined results of all ZRIDs.
The structure depends on the output of \code{get_az_valid_to()}.
}
\description{
This function calls \code{get_az_valid_to()} for multiple ZRIDs in parallel
to efficiently retrieve verified time periods. Parallel execution is handled
via the \pkg{furrr} package.
}
\details{
The function uses \code{future_map()} from the \pkg{furrr} package to perform
parallel execution. Each ZRID is processed using the helper function
\code{process_zrid()}, which wraps a call to \code{get_az_valid_to()} inside
a \code{tryCatch()} block for robust error handling.

If a ZRID fails, a warning message is displayed and a row with \code{NA} values
is returned for that ZRID instead of stopping the entire process.
}
\examples{
\dontrun{
result <- get_verified_periods_parallel(
  hub = my_hub_connection,
  zrids = c("ZRID001", "ZRID002"),
  begin = "2024-01-01",
  end = "2024-12-31",
  workers = 8
)
}

}
\seealso{
\code{\link[furrr:future_map]{furrr::future_map()}}, \code{\link[future:plan]{future::plan()}}, \code{\link[=get_az_valid_to]{get_az_valid_to()}}
}
